<% if @feed_items.any? %>
  <ol class="works">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items, class: "pagination center",
                    params: { controller: :static_pages, action: :home } %>
<% end %>
